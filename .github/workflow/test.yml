name: Simple Test Workflow

on:
  push:
    branches:
      - main
jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello there, Hi from Github Actions!"
